{
  "name": "@cross-nn/browser",
  "version": "0.0.0",
  "description": "Browser module of cross-platform neural network",
  "main": "./browser.js",
  "typings": "./browser.d.ts",
  "author": "Andrey Vasin <vofuss@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vofus/cross-nn.git",
    "directory": "packages/browser"
  },
  "scripts": {
    "prepare": "npm run build",
    "watch": "npm run clean && tsc -w",
    "build": "npm run clean && tsc",
    "clean": "rimraf dist",
    "dev": "npm run clean && parcel serve test/index.html --out-dir dist/test"
  },
  "dependencies": {
    "@cross-nn/core": "0.0.4"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
